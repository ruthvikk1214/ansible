- name: play level variables
  hosts: local
  connection: local 
  vars_files:
  - variables2.yaml
  tasks:
  - name: print variables
    ansible.builtin.debug:
      msg: " Hello, my name is {{Name}}, age is {{Age}} and i weigh {{Weight}} kilos"